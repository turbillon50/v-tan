# ESTADO DEL SISTEMA — 17 de agosto de 2026

**Fase 0 de la auditoría**: verificar qué sigue vivo tras ~3 meses de inactividad.
Último commit del repo: `ea26775`, **2026-05-23**. Rama auditada: `claude/proyecto-adita-3w56r2`
(idéntica a `main`).

Esta corrida es **solo verificación**. No se modificó código, ni gobernanza, ni el kill
switch, ni se ejecutó ninguna operación en Bybit.

---

## Resumen

| Punto | Resultado |
|---|---|
| `pnpm install` | ✅ OK — lockfile al día, 674 paquetes, 12.5 s |
| `pnpm run typecheck` | ❌ **FALLA — 152 errores** en 10 archivos |
| `pnpm --filter @workspace/api-server run build` (esbuild) | ✅ OK — bundle de 16.4 MB |
| Bugs de runtime confirmados | 🔴 **3**, uno de ellos en la última feature que se subió |
| Neon / Postgres | ⚠️ **Sin verificar** — falta `DATABASE_URL` |
| API Railway / proxy Bybit | ⚠️ **Sin verificar** — la red de este entorno bloquea la salida |
| Frontend `tanit.work` | ⚠️ **Sin verificar** — misma razón |
| Cuenta Bybit | ⚠️ **Sin verificar** — faltan llaves de lectura |

**Conclusión de la fase**: el proyecto **instala y empaqueta**, pero **no compila con
comprobación de tipos**, y esos errores no son cosméticos — tres son fallos reales en
ejecución. Lo que sigue vivo en la nube no se pudo confirmar desde aquí.

---

## 1. Instalación y compilación

### `pnpm install` — OK
```
Lockfile is up to date, resolution step is skipped
Packages: +674
Done in 12.5s using pnpm v10.33.0
```
Entorno: Node 22.22.2, pnpm 10.33.0 (coincide con `packageManager` en `package.json`).
Las versiones instaladas salen del lockfile, no de una resolución nueva — o sea, **los
errores de abajo ya existían cuando se hizo el último push**, no son deriva de dependencias.
Versiones confirmadas: `@mastra/core` 1.32.1, `@mastra/memory` 1.17.5, `@mastra/pg` 1.10.0,
`zod` 3.25.76, `typescript` 5.9.3.

### `pnpm run typecheck` — FALLA con 152 errores

Las librerías (`lib/db`, `lib/api-zod`, `lib/api-client-react`, `lib/integrations-gemini-ai`)
pasan. Todo el fallo está en `artifacts/api-server`:

| Archivo | Errores |
|---|---:|
| `src/mastra/tools/bybit-write-tools.ts` | 96 |
| `src/mastra/tools/bybit-tools.ts` | 22 |
| `src/lib/vercel.ts` | 13 |
| `src/lib/self-edit.ts` | 6 |
| `src/lib/trading-engine.ts` | 4 |
| `src/lib/railway.ts` | 4 |
| `src/mastra/tools/governance-tools.ts` | 3 |
| `src/lib/voice-live.ts` | 2 |
| `src/routes/admin.ts` | 1 |
| `src/mastra/tools/image-tools.ts` | 1 |

Por código: 77× TS2322 (`unknown` no asignable), 29× TS18046, 20× TS2339, 12× TS2345,
8× TS2304 (nombre no encontrado), 4× TS2353, 1× TS2783, 1× TS2554.

El grueso (118 de 152, en las tools de Mastra) es el mismo patrón: el `context` de las tools
llega tipado como `unknown`, así que cada `context.symbol`, `context.size_usd`, etc. falla.
Es un desajuste con los tipos de `@mastra/core` 1.32.1 — molesto pero probablemente inocuo en
ejecución. Los 13 de `vercel.ts` + 4 de `railway.ts` + 6 de `self-edit.ts` son casi todos
`HeadersInit` no declarado (`@types/node` no lo expone con `"types": ["node"]`); existe en
Node 22 en runtime.

**Los que sí importan están abajo.**

### `pnpm --filter @workspace/api-server run build` — OK

```
dist/index.mjs   16.4mb ⚠️
⚡ Done in 1626ms
```

**Aquí está el detalle grave de proceso**: `build.mjs` es esbuild puro, sin `tsc`. El
`railway.json` usa exactamente ese comando:

```json
"buildCommand": "pnpm install && pnpm --filter @workspace/api-server run build"
```

O sea **Railway construye y despliega aunque el typecheck esté roto**. El único comando que sí
verifica tipos es `pnpm run build` de la raíz (`typecheck && -r build`), y ese no lo usa
ninguna plataforma. Es la razón por la que los 3 bugs de abajo llegaron a producción sin que
nada los frenara.

---

## 2. Bugs de runtime confirmados

### 2.1 🔴 `text` no declarada — la ruta OpenRouter del motor legacy nunca funcionó

`artifacts/api-server/src/lib/trading-engine.ts:8487, 8498, 8500, 8525`, dentro de
`runGeminiUserCommand()` (declarada en la línea 5273). No hay ninguna declaración de `text` en
ese ámbito — la más cercana (`let text` en la 7268) pertenece a otra función.

El módulo es ESM (`"type": "module"` + salida ESM de esbuild), así que corre en modo estricto.
Verificado en este mismo entorno con Node 22:

```
READ  -> ReferenceError: text is not defined
WRITE -> ReferenceError: text is not defined
```

Efecto concreto cuando `OPENROUTER_API_KEY` está configurada y se entra a esa rama:

1. Línea 8487, `text = orRaw;` — dentro del `try` del bucle de modelos. Lanza
   `ReferenceError`, lo traga el `catch (orErr)` de la línea 8495 y se loguea como
   *"[OR-PRIMARY] modelo falló"*. **La respuesta buena de OpenRouter se descarta**, aunque el
   log de la línea anterior ya dijo "✅ exitoso".
2. Línea 8498, `if (text)` — **fuera** del try. Lanza `ReferenceError` sin nada que lo atrape.

Esto viene del commit `c210789 feat: OpenRouter como primary — Gemini ya no es requerido`
(2026-05-15), la **última feature subida antes de que el repo se detuviera**. La ruta que se
suponía que iba a hacer innecesario a Gemini nunca ejecutó correctamente ni una vez.

### 2.2 🔴 `API_URL` no declarada — búsqueda semántica de imágenes rota

`artifacts/api-server/src/mastra/tools/image-tools.ts:221`

```ts
url: imgId > 0 ? `${API_URL}/image/${imgId}` : "",
```

`API_URL` no está declarada ni importada en ningún punto del archivo (única aparición: esa
línea). `ReferenceError` en cuanto la tool devuelve al menos un resultado.

### 2.3 🟠 El prompt de voz recibe `[object Promise]`

`artifacts/api-server/src/lib/voice-live.ts:46`

```ts
governance = rulesAsPromptText(rules);
```

`rulesAsPromptText()` (`lib/governance.ts:228`) es `async` y **no recibe argumentos**. Dos
fallos en una línea: se le pasa un argumento de más (TS2554) y no se le hace `await`. Como
está dentro de un `try/catch`, no rompe nada — simplemente inyecta `[object Promise]` donde
debería ir el resumen de gobernanza. La Tanit de voz **no conoce sus reglas**.

### 2.4 🟡 Menor

`routes/admin.ts:822` — la propiedad `ok` se especifica dos veces en el mismo objeto literal;
la segunda pisa a la primera (TS2783).

---

## 3. Infraestructura — no verificable desde este entorno

Ninguno de estos puntos se pudo comprobar. **No hay que leerlos como "está caído"**, sino como
"sigue sin saberse".

- **Neon / Postgres**: no hay `DATABASE_URL` en el entorno y no está en el repo (correcto que
  no esté). Sigue sin responderse la pregunta más importante de la fase: si la instancia
  sobrevivió 3 meses de inactividad. Recordatorio del hallazgo #9 de la auditoría: ~14 tablas
  que el código usa no existen en ninguna migración, así que si esa base se perdió, no hay
  forma de recrear el esquema desde el repo — solo un import parcial desde
  `tanit-alma-completa.json` (export de abril).
- **API Railway y proxy Bybit**: la política de red de este entorno responde 403 al CONNECT
  hacia `motivated-prosperity-production-8a64.up.railway.app` y
  `api-money-maker-m2m.railway.app`. Es el sandbox, no los servicios.
- **`tanit.work`**: mismo bloqueo. Consultando la API de Vercel con el token disponible, la
  lista de proyectos de "luis' projects" (tope de 50, y hay al menos 50) **no incluye ninguno
  llamado tanit** — pero al estar la lista topada, eso no prueba nada.
- **Cuenta Bybit**: sin llaves de lectura no se pudo mirar equity ni posiciones abiertas. Sigue
  sin confirmarse si quedó alguna posición huérfana de mayo.

---

## 4. Qué hace falta para cerrar la Fase 0

Con esto se responde todo lo que quedó abierto:

1. `DATABASE_URL` de Neon → conteos de las 8 tablas críticas y lectura (solo `SELECT`) del
   estado real de gobernanza.
2. URL real del API en Railway y del frontend → comprobación de salud y qué commit está
   desplegado.
3. Llaves de Bybit de solo lectura, o el `PROXY_SECRET` vigente → equity y posiciones abiertas.

---

## 5. Recomendación inmediata (no ejecutada — requiere tu visto bueno)

Independientemente de lo que se decida sobre gobernanza y autenticación, hay un cambio de una
línea que evita que esto se repita: que **Railway construya con el comando que sí verifica
tipos**. Hoy `railway.json` salta el `tsc`, y por eso tres bugs de runtime llevan tres meses
desplegados sin que nadie se enterara.

Los tres bugs de la sección 2 son arreglos pequeños y acotados. No los toqué porque el alcance
acordado para esta corrida era solo verificar.

---

*Fase 0 de la auditoría de 2026-08-17. Los hallazgos de seguridad y de riesgo operativo
(API sin autenticación, guard de `/admin` evitable, `PROXY_SECRET` commiteado, frenos
desactivados, tres motores sin coordinar) están en el plan de auditoría, no aquí. Gobernanza
no se tocó, por decisión de Luis.*
