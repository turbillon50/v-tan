# Override Nixpacks defaults so Railway works with pnpm catalog/overrides.
# Default `pnpm i --frozen-lockfile` fails when the lockfile was generated by
# a slightly different pnpm version, even when no real dependency changes exist.

[phases.install]
cmds = ["pnpm install"]
